name: xpulse
description: "XPulse — synchronized biometrics, collective progression."
publish_to: 'none'

version: 0.1.0+14

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  health: ^12.0.0
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.3.2
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/sample_user.json
    - assets/characters/
